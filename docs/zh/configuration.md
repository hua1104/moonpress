---
title: 配置说明
description: MoonPress 支持的页面元数据
order: 4
lang: zh
---

# 配置说明

MoonPress 通过 front matter 读取页面级配置。

## 页面字段

- `title`：页面标题，用于导航、HTML 标题和搜索索引。
- `description`：页面简介，用于搜索索引和后续元数据。
- `order`：导航和上一篇/下一篇的排序值。
- `draft`：设为 `true` 时跳过该页面。
- `lang`：页面语言，英文为 `en`，中文为 `zh`。

